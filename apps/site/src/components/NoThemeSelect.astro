---
/**
 * Starlight's theme control, removed.
 *
 * The site has one palette until somebody decides whether a dark documentation is worth
 * designing properly. A switch that moves between a theme and a copy of itself is worse than
 * no switch.
 */
---
